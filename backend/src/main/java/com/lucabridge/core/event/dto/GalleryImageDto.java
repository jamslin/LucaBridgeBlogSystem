package com.lucabridge.core.event.dto;

/** No alt text yet: MediaText has no repository or admin editing surface until that CMS work lands. */
public record GalleryImageDto(String url, Integer width, Integer height) {
}
